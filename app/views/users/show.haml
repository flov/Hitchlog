%h1= @user.username.capitalize
%p
  = "#{@user} logged #{pluralize(@user.hitchhikes.size, 'hitchhike')}"
  = "and travelled #{@total_distance} kms by hitchhiking."
  %br
  = "#{@user} waited averagely #{@average_waiting_time} minutes per ride."
  %br
  = "All in all #{@user} waited #{@total_waiting_time} minutes for #{pluralize(@waiting_time.size, 'ride')}."
%ul
  - for trip in @user.trips
    %li
      = link_to raw("#{trip.from} &rarr; #{trip.to} #{distance(trip.distance)}"), trip_path(trip)
