%h1 Hitchhikes:
%ul
  - for hitchhike in @hitchhikes
    %li
      = link_to raw("#{hitchhike.from} &rarr; #{hitchhike.to} (#{distance(hitchhike)})"), hitchhike_path(hitchhike)
      by: 
      = link_to hitchhike.user.username, user_path(hitchhike.user.username)